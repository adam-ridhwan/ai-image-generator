import GeneratedImageDialog from '@/components/generated-image-dialog';
import Prompt from '@/components/prompt';

const BuyCreditsPage = () => {
  return (
    <main className='mb-20'>
      <span>buy credits page</span>
    </main>
  );
};

export default BuyCreditsPage;
